use competitive::prelude::*;

#[argio(output = AtCoder)]
fn main(n: usize; ab: [(usize, usize); n]) -> bool {
    let mut memo = vec![0; 2 * n];
    let mut able = Vec::new();
    for i in 0..n {
        //
    }
    false
}
